name: psgd-ensemble
channels:
  - conda-forge
dependencies:
  - python=3.8
  - numpy
  - pylint
  - scikit-learn
  - pip
  - pandas
  - setuptools
  - plotly
  - jupyter
  - tqdm
  - pip:
    - jax
    - jaxlib
    - tqdm
    - git+https://github.com/online-ml/river
    - -e file:prime
    - -e file:experiment_runner
    